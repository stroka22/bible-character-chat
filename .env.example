# Bible Character Chat - Environment Variables
# Copy this file to .env and fill in your own values

# Supabase Configuration
# Find these in your Supabase project settings > API
#
# REQUIRED ----------------------------------------------------------
# Replace the placeholders with values from your own project.
# Example project-ref shown below (DO NOT copy it verbatim unless
# your Supabase project ref really is `sihfbzltlhkerkxozadt`).
#
VITE_SUPABASE_URL=https://sihfbzltlhkerkxozadt.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# OpenAI Configuration
# OPTIONAL ----------------------------------------------------------
# Chat features use GPT-4.  If you don’t need AI responses you may
# leave this blank and the app will gracefully fall back.
# Get your API key from https://platform.openai.com/account/api-keys
# (Never commit real keys to a public repository.)
#
# ***REMOVED***
***REMOVED***
# In production, NEVER expose your OpenAI API key directly to the browser.
# Instead, use a server-side API or Supabase Edge Functions to proxy requests
# to OpenAI and keep your API key secure.

# ------------------------------------------------------------------------------
# Stripe Configuration
# ------------------------------------------------------------------------------
# These keys/IDs must come from the SAME Stripe environment.
# For production use your LIVE keys & price IDs.
# For local testing you may comment-out the live values and uncomment the TEST
# values instead.

# --- LIVE MODE (production) ---  (REQUIRED for production billing)
VITE_STRIPE_PUBLIC_KEY=pk_live_your-public-key
VITE_STRIPE_PRICE_MONTHLY=price_live_monthly-id
VITE_STRIPE_PRICE_YEARLY=price_live_yearly-id

# Server-side secret key ‑ NEVER prefix with VITE_ (kept only on server / CI)
STRIPE_SECRET_KEY=sk_live_your-secret-key

# --- TEST MODE (local / staging) --- (recommended for local dev)
# Uncomment the four lines below and comment-out the live section above if you
# want to run in Stripe *test* mode.
# VITE_STRIPE_PUBLIC_KEY=pk_test_your-public-key
# VITE_STRIPE_PRICE_MONTHLY=price_test_monthly-id
# VITE_STRIPE_PRICE_YEARLY=price_test_yearly-id
# STRIPE_SECRET_KEY=sk_test_your-secret-key

# Optional helper variables (used for Supabase Edge Function checkout)
# If you leave these blank the app will fall back to a Vercel API route.
#
# VITE_SUPABASE_PROJECT_REF=sihfbzltlhkerkxozadt
# VITE_SUPABASE_EDGE_FUNCTION_URL=https://sihfbzltlhkerkxozadt.functions.supabase.co/create-checkout-session

# -------------------------------------------------------------------
# Developer Options
# -------------------------------------------------------------------
# Enable extremely verbose auth / route logs in the browser console.
# VITE_AUTH_DEBUG=true
